[package]
name = "textc"
version = "0.0.1"
edition = "2024"

[dependencies]
bincode = "1.3"
clap = {version = "4.5.60", features = ["derive"]}
serde = { version = "1.0.228", features = ["serde_derive"] }
